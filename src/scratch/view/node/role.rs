#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum Role {
    Root,
    Stack,
    MenuBar,
    Menu,
    Binding,
    Separator,
    TextArea,
    Button,
    Checkbox,
    Radio,
    Slider,
    TextBox,
    Scroll,
    Panel,
    FloatingPanel,
    SectionHeader,
    Label,
}
